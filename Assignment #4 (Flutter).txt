import 'package:flutter/material.dart';

void main() {
  runApp(AssignmentFour());
}

class AssignmentFour extends StatelessWidget {
  const AssignmentFour({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Assignment#4_(Flutter)',
      home: HomePage(),
    );
  }
}

class HomePage extends StatelessWidget {
  const HomePage({super.key});

  @override
  Widget build(BuildContext context) {

    double fullScreenWidth = MediaQuery.of(context).size.height;
    double fullScreenHeight = MediaQuery.of(context).size.height;

    return Column(
      children: [
        Container(
          width: fullScreenWidth,
          height: fullScreenHeight * 0.2,
          color: Colors.red,
          child: Center(child: Text(
            '20%',
            style: TextStyle(color: Colors.black, decoration: TextDecoration.none, fontSize: 22),
          )),
        ),

        Container(
          width: fullScreenWidth,
          height: fullScreenHeight *0.8,
          color: Colors.green,
          child: Center(child: Text(
            '80%',
            style: TextStyle(color: Colors.black, decoration: TextDecoration.none, fontSize: 22),
          )),
        ),
      ],
    );
  }
}